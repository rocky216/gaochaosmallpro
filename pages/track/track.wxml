<!--pages/track/track.wxml-->
<view class='track'>
  <view class='track_item' wx:for="{{trackList}}" wx:key="{{index}}">
    <navigator url="/pages/trackItem/trackItem?type={{item.id}}">
      <text>{{item.name}}</text>
    </navigator>
  </view>
</view>
